<!DOCTYPE html>
<html>
  <head>
    <title>Hoy Trabajas - RoR Test</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!--Bootstrap CDN  -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <div class="container">
      <%= yield %>
      <p class="notice"></p> 
      <div style="bottom: 25px; left: 25px; position: absolute">  
        <% if notice%>
          <div class="alert alert-success alert-dismissible fade show" role="alert" style="max-width: 350px; min-width: 250px;">
            <%= notice %>
            <button data-bs-dismiss="alert" type="button" class="btn-close" aria-label="Close"></button>
          </div>
        <% end %>
        <% if alert != nil %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert" style="max-width: 350px; min-width: 250px;">
            <%= alert %>
            <button data-bs-dismiss="alert" type="button" class="btn-close" aria-label="Close"></button>
          </div>
        <% end %>
        <% if flash[:errors] %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert" style="max-width: 350px; min-width: 250px;">
            <% flash.each do |name,message| %>
              <%= message%>
            <% end %>
            <button data-bs-dismiss="alert" type="button" class="btn-close" aria-label="Close"></button>
          </div>
        <% end %>
      </div>
    </div>    
  </body>
</html>
